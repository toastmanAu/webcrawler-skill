# Memory — 2026-03-11

## Fiber Installer — Major Session (continuing from 2026-03-10)

### Root Cause Fixes Applied
- **Blank config dirs bug**: Two separate `set -e` exits before config was written:
  1. `write_config`: curl fetch of upstream base config was not `|| true` — network fail = silent exit
  2. `install_service`: `systemctl --user` fails when running as root → exited before config written
- Both fixed in commit `7ba749f`

### Root User Support (commit `7ba749f`)
- Installer now detects `id -u == 0` throughout
- Root → system service at `/etc/systemd/system/` using plain `systemctl`
- Non-root → user service at `~/.config/systemd/user/` using `systemctl --user`
- Same applies to `install_dashboard`, `verify_install`, summary output
- Philosophy: "installer figures out the environment, user shouldn't need to know if they're root"

### Start Node/Dash Prompt After Smoke Test (commit `47a060d`)
- After smoke test (pass or fail), installer now offers:
  - "Start the Fiber node now? [Y/n]" → defaults Y
  - "Start the dashboard now? [Y/n]" → defaults Y, prints live URL
- Independent of smoke test result

### Dashboard Settings Editor (commit `47a060d`)
- "⚙️ Edit Settings" in Maintenance grid opens modal
- Edit CKB full node URL without touching config.yml manually
- Pre-fills current value, saves + auto-restarts node
- Backend: `edit-config` maintenance action (regex patch of config.yml)
- `maint()` JS helper accepts extra payload fields now

### Dashboard OTA Update (commit `22f32ac`)
- `DASHBOARD_VERSION = "1.2.0"` constant added
- "🔄 Check for Updates" button in Maintenance grid
- Checks fnn binary vs nervosnetwork/fiber latest release
- Checks dashboard vs latest raw file on GitHub
- OTA: fetches latest fiber-dash.py, backs up `.bak`, replaces self, restarts service, page auto-refreshes
- `dashboard_version` exposed in `/api/control_status` response

### Current fiber-installer commits (master)
- `22f32ac` — OTA update + check for updates
- `47a060d` — start prompt + settings editor
- `7ba749f` — root user support + config write resilience
- `31cec54` — full UX rewrite (prev session)

### Zero 3 Status (as of 00:18 ACST)
- Re-running installer with correct CKB RPC (`http://192.168.68.87:8114`)
- Second run slower than expected — still in progress
- Root user confirmed (was `root` not a regular user)

### Release Plan
- Target 1.0 release once Zero 3 (aarch64 root) + Windows both confirm clean
- Test matrix for 1.0: aarch64 Linux root, aarch64 Linux user, x86_64 Linux, Windows
- OTA update = hotfix mechanism during hackathon week (push to master → users hit "Check for Updates")

### Hackathon
- Starts in ~23 hours from 2026-03-10 23:48 ACST
- Plan: post installer announcement before Phill goes to bed tonight
- Windows test still pending

## Design Principles Established
- Scripts should handle root or user install inclusively — no "don't run as root" warnings
- Installer's job: figure out the environment, adapt, complete without user needing to know internals
- OTA update loop: push fix → users self-update via dashboard button, no re-install needed
- Bug report in all public tools (fiber-dash already has `_handle_bug_report` → GitHub issues)

## WyMesh — Concept Crystallised (2026-03-11 ~01:15 ACST)

Working name: **WyMesh** (or CKB Mesh / ProofNet — TBD)

### Core Concept
A decentralised IoT mesh network where physical relay nodes earn CKB for useful work (packet relay, coverage provision) with hardware-level trust anchored on-chain via CKB DOBs.

### Stack
- **Hardware**: ESP32 + SX1276 LoRa (TTGO LoRa32 ~$15, solar-chargeable)
- **Mesh protocol**: Meshtastic (open source, ESP32-native, active community)
- **Identity**: ATECC608A secure element — unforgeable device keypair (~$1)
- **On-chain identity**: DOB minted per device containing pubkey + firmware hash + HW revision
- **Transport auth**: WyAuth redirects routed over LoRa mesh → gateway → JoyID → back
- **Proof**: "Proof of Coverage" / "Proof of Relay" — signed witness reports submitted to CKB
- **Validation**: Type script checks firmware hash IN approved whitelist cell before paying
- **Rewards**: Fiber micropayments per packet relayed or per uptime epoch
- **Settlement**: CKB L1 anchors node registry, staking, whitelist governance

### Firmware Trust Model
- Device DOB contains firmware hash at flash time
- Whitelist cell (governed by multisig/community) = approved firmware hashes
- Type script: `device_dob.firmware_hash IN whitelist AND proof.signature.valid`
- Wrong firmware hash = proof rejected = no reward
- Firmware update = new hash added to whitelist cell; old versions can be revoked
- No centralised firmware server, no manufacturer backdoor

### WyAuth Integration
- LoRa mesh becomes a transport layer for WyAuth sign requests
- Device with no WiFi authenticates via mesh → gateway → JoyID → back
- Relay nodes earn CKB for forwarding WyAuth packets too
- Geographic routing: Fiber channels between mesh nodes in different cities

### Why CKB
- Cell model = perfect for coverage proof cells (compact, composable)
- CKBFS = permanent coverage map storage, verifiable by anyone
- Fiber = micropayment throughput for per-packet rewards
- DOB = transferable device identity with provenance history
- Type scripts = trustless firmware verification, no central authority

### Key Insight
"Not a validator, not a traditional miner — a physical infrastructure participant. The transport layer has geography. You can prove a message traversed a specific region of the planet."

### Next Steps
- Research task `ckb-lora-proof-of-coverage` queued (MEDIUM priority)
- Prototype BOM: TTGO LoRa32 + ATECC608A + BME280 (optional sensor) + solar panel
- Design DOB schema for device identity
- Sketch type script validation logic
- Consider FiberQuest submission angle

## Zero 3 fiber fix — root cause + resolution (02:30 ACST)
- **Root cause**: installer was run once as non-root (created `~/.config/systemd/user/fiber.service` with `pkill -9 fnn`), then re-run as root (created `/etc/systemd/system/fiber.service`). User-level service kept killing system-level fnn every 10s restart cycle.
- **Fix**: removed user-level service, fixed system service with `FIBER_SECRET_KEY_PASSWORD`, `--dir` flag, ckb key at `$BASE_DIR/ckb/key`
- **Installer fixed**: `afc181f` — now cleans up conflicting user-level service when running as root, adds password env var, adds `--dir`, populates `ckb/key` from `data/key`
- **Zero 3 status**: fiber + fiber-dash both active, enabled, running on mainnet
- **Dashboard**: http://192.168.68.98:8229

## fiber-rpc-js v0.2.0 (03:00 ACST)
- Added 11 methods: connectPeer, disconnectPeer, acceptChannel, shutdownChannel, updateChannel, parseInvoice, cancelInvoice, graphNodes, graphChannels, buildRouter, sendPaymentWithRouter
- 39/39 tests passing live against ckbnode
- Commit: d7d81dd → toastmanAu/fiber-rpc-js

## Local repo mirrors created
- /home/phill/mirrors/ — ckb-sdk-js, ccc, joyid-sdk-js, molecule, ckb-js-toolkit
- Fiber mono-repo updated to latest develop (3e01958e)
- See /home/phill/mirrors/README.md for refresh instructions

## Fiber Dashboard Debugging Session (2026-03-11 morning)

### Zero 3 Fiber Node (ckb-zero3, 192.168.68.98)
- fiber v0.7.1 running on mainnet, P2P 8228, RPC 127.0.0.1:8227
- Dashboard: http://192.168.68.98:8229 (fiber-dash.service, system, root)
- Dashboard v1.3.0 deployed and working

### Key Finding: fnn v0.7.1 RPC Auth
- v0.7.1 added `BiscuitAuthMiddleware` — method names in rules use short form (`node_info`) but JSON-RPC methods match exactly
- Without biscuit_public_key in config: auth is disabled — works fine
- Earlier "Unauthorized" was from testing with wrong method name (`get_node_info` instead of `node_info`)
- No auth issue in practice — was a false alarm

### Dashboard Issues Fixed (commits b41d1c3 → 7690f52)
- **Turn-key fix**: `fiber-dash.service` now has `ExecStartPre` that waits up to 30s for fnn RPC port before starting
- **Full flags**: installer now passes `--control`, `--data-dir`, `--fnn-bin`, `--network`, `--biscuit`
- **Biscuit key UI**: settings modal has Biscuit Public Key field; `edit-config` handles it; `control_status` exposes it
- **Boot console**: static HTML overlay (not JS-created) shows each startup step — auto-dismisses on success
- **Safari compat**: removed template literals, Promise.allSettled, Object.assign from boot console
- **Fetch timeouts**: 8s AbortController timeout on all fetch calls
- **Dashboard v1.3.0** live on Zero 3

### Root Cause of "not loading" on phone
- Was intermittent network — phone couldn't reach Zero 3 consistently
- Zero 3 is reachable fine (health endpoint confirmed)
- Real fix: static DHCP leases needed across all node machines

### Static DHCP Reservations (to add in Deco X73 app)
- Zero 3: `02:00:f0:b2:da:16` → 192.168.68.98
- ckbnode eth0: `00:00:a4:3d:5f:7b` → 192.168.68.105
- ckbnode wlan0: `e0:51:d8:20:df:76` → 192.168.68.87
- N100 eth: `20:24:04:18:19:93` → 192.168.68.76
- NucBox eth: `c8:ff:bf:01:4f:e5` → 192.168.68.79
- Pi5 eth: `86:2e:79:62:b4:39` → 192.168.68.82
- Deco app: More → Advanced → DHCP Server → Address Reservation
- Phill doing firmware update on Deco X73 first (15 new features)

### Snapshot Pipeline (v6)
- v5 killed (was stuck in restart loop since Mar 7)
- v6.sh launched at 09:14 ACST on NucBox
- v6 optimisations: no -z compression, aes128-gcm SSH cipher, zstd -15, staging in /home/phill/
- Still rsyncing as of 10:15 — CKB node restarted mid-rsync, causing slow convergence
- Staging: /home/phill/ckb-snapshot-staging/ on NucBox

## Fiber Installer — v1.0 Release Prep (10:30-10:45 ACST)

### Critical Bug Fixed: bare await crash
- `openSettings()` function body existed but `async function openSettings() {` declaration was missing
- The `await fetch(...)` inside was at top-level of script → silent SyntaxError on strict JS engines
- Caused "Loading JavaScript…" to hang — 1-in-30 success rate (lenient browsers only)
- Fixed in commit `7d69094` — added `async function openSettings()` wrapper
- Verified with `node --check` — now enforcing JS syntax check before every commit
- Dashboard loads instantly on Chrome and Safari after fix ✅

### Windows Test Needed Before 1.0
- Windows installer: `irm https://raw.githubusercontent.com/toastmanAu/fiber-installer/main/install.ps1 | iex`
- EliteDesk is Ubuntu 22.04 — not a Windows test machine
- No Windows machine currently available to Phill
- Options: VM, Windows Sandbox, or ship 1.0 with Windows "untested" and verify on first report
- Linux (x86_64) and aarch64 (Zero 3) confirmed working ✅

### fiber-installer commit history (this session)
- `b41d1c3` — turn-key dash: service wait, full flags, biscuit key settings
- `138aee0` — boot console (auto-dismissing startup log)
- `454fdb5` — Safari compat (no template literals/Promise.allSettled in boot console)
- `d7df1e5` — boot console as static HTML (visible before JS runs)
- `7690f52` — 8s fetch timeout on all API calls
- `7d69094` — fix bare await crash (the real bug)

### Snapshot v6 Pipeline (manual restart)
- CKB stopped on ckbnode at ~10:20 ACST
- snapshot-manual.sh launched on NucBox, rsync running clean
- At 85% as of 10:38 — should complete rsync ~10:50, then compress + upload
- Will Telegram notify Phill on completion
- Script: /home/phill/snapshot-manual.sh on NucBox (192.168.68.79)

### Deco X73 — Static DHCP Reservations Set ✅
- 11 machines reserved in Deco app (More → Advanced → DHCP Server → Address Reservation)
- Key ones:
  - Zero 3: 02:00:f0:b2:da:16 → 192.168.68.98
  - ckbnode eth0: 00:00:a4:3d:5f:7b → 192.168.68.105
  - ckbnode wlan0: e0:51:d8:20:df:76 → 192.168.68.87
  - N100 eth: 20:24:04:18:19:93 → 192.168.68.76
  - NucBox eth: c8:ff:bf:01:4f:e5 → 192.168.68.79
  - Pi5 eth: 86:2e:79:62:b4:39 → 192.168.68.82
- Deco firmware also updated (15 new features)

## EliteDesk Disk Layout (confirmed 2026-03-11)
- Boots Ubuntu 22.04 from NVMe: nvme0n1p1 (232.9GB, 104GB used, 114GB free)
- sda (111.8GB) = Windows drive, STILL INTACT — NOT wiped
  - sda1: 100MB vfat EFI
  - sda2: 16MB (MSR)
  - sda3: 111.2GB NTFS (Windows C: drive)
  - sda4: 537MB NTFS (recovery)
- sdb (447.1GB ext4) = extra storage, unmounted
- Windows partition is bootable (EFI + full NTFS install present)
- Can boot Windows for fiber-installer PowerShell testing

## Windows Installer (PS1) — Fixes (11:00-11:26 ACST)

### Issues Found & Fixed
1. **404 on `main` branch** — repo uses `master` not `main`
   - Correct URL: `irm https://raw.githubusercontent.com/toastmanAu/fiber-installer/refs/heads/master/install.ps1 | iex`
2. **PS1 was 8+ months behind Linux** — rewrote from scratch in commit `102a06f`
   - Now feature-parity: mainnet/testnet/both, dashboard, NSSM service, smoke test, firewall, PATH
3. **`??` null coalescing operator** — PS5.1 doesn't support it (needs PS7+)
   - Fixed in commit `67d0598` — replaced with `if ($x) { $x } else { "default" }`
4. **EliteDesk Windows: WiFi adaptor not working** — used ethernet instead
5. **Windows suspended mid-test** — power settings on that install are aggressive

### Windows Test Environment
- EliteDesk HP 800 G1 DM running Windows 10 22H2 (build 19045)
- PowerShell 5.1 (default — NOT PS7)
- WiFi driver not working on Windows; ethernet works fine
- Grub2Win bootloader, EFI boot

### PS5.1 Compatibility Rules (for future PS1 work)
- No `??` — use `if ($x) { $x } else { "default" }`
- No `?.` optional chaining
- No ternary `?:` — use `if/else`
- No `[x, y]` array literals in some contexts
- `$ErrorActionPreference = "Stop"` works fine
- `Invoke-WebRequest` + `-UseBasicParsing` for all web calls

### Snapshot v6 — Still Running
- On SST file 173372.sst as of 11:25 ACST
- Still 2 processes active
- CKB stopped on ckbnode throughout — clean delta transfer
- Very slow (20-100 KB/s) due to delta-heavy SST files
- Will Telegram notify on completion

## OPi3B Snapshot Node Plan (2026-03-11)
- **Board**: Orange Pi 3B (has eMMC, M.2 M-key slot supports 2230/2242 NVMe + SATA)
- **Drive**: Phill has a spare 1TB 2230 NVMe — currently unused, was probably bought for OPi3B
- **Plan**: Boot from eMMC, slot 1TB NVMe into M.2 → dedicated CKB snapshot staging node
- **Role**: rsync target from NucBox during snapshot, compress + upload from OPi3B
- **Replaces**: NucBox staging dir (which competes with NucBox's own disk)
- **Status**: TODO — do after Windows fiber installer is sorted
- OPi5+ has no eMMC (NVMe-only boot), so not suitable for freeing M.2
- OPi5+ M.2 slot is 2280, so 2230 drive doesn't fit well there anyway
