# Memory — 2026-03-04

## CKBFS + DOB Minting Pipeline — COMPLETE ✅

### What was accomplished
- Published 76KB Founding Member JPEG to CKBFS on CKB testnet
  - TxHash: `0x5b3b76088ff6686552c85e6854b31ab13712bf5063486fef4b20c63973679c6f`
  - TypeID: `0x061cc843e720a4274da71a1a2fec4802cd02f6d5ee7167e7429435878068abe0`
  - Cost: 225 CKB locked (vs 78,066 CKB naive per DOB) — 260x cheaper
- Minted 4 testnet DOBs with `contentType: application/ckbfs` referencing the CKBFS TypeID
- Key lesson: wrap `@ckbfs/api` SDK — hand-rolled molecule encoding was subtly wrong
- Contract `0x31e637...` = CKBFS V2 (deployed 20241025)

### Mainnet plan
- 100 Founding Member DOBs (decided this session)
- Cost: ~30,225 CKB (~$151 AUD) — 100 × 300 CKB DOBs + 225 CKB CKBFS
- Command: `node mint.js --ckbfs --mainnet` (once wallet funded)

## CKBFS Viewer — Built & Deployed ✅
- URL: `wyltekindustries.com/ckbfs-viewer.html`
- Standalone React/Vite repo: `toastmanAu/ckbfs-viewer`
- Works Chrome, Brave, Safari iOS
- Key fixes: multi-chunk witness reassembly, base64 data URL for Safari, molecule Vec/Bytes decode

## JoyID + Supabase Auth — WORKING ✅ (Chrome + Safari)
- auth-test.html: JoyID connect → CKB address → Supabase anonymous auth
- Anonymous sign-ins enabled in Supabase dashboard
- Session persisted in localStorage keyed by CKB address
- JoyID SDK loaded via esm.sh (jsdelivr .mjs had bare specifier imports that broke in browser)
- Brave: popup blocked by default — low priority
- Safari: glitchy (popup loses focus, user has to tap twice) — acceptable for now
- Supabase project: yhntwgjzrzyhyxpiqcts

## Membership System — IN PROGRESS
### Plan agreed
- 100 founding members get a DOB minted to their JoyID wallet automatically (prepaid by Phill)
- Flow: user connects JoyID → CKB address captured → Supabase account created → mint queue entry → Pi runs mint script → DOB sent to their address
- Member-only pages: Research, CKBFS Viewer, DOB Minter (hidden from nav for non-members)
- Nav: add Members link (visible to all as login/signup entry point)

### Next steps
1. Build `members.html` — proper signup/login page replacing auth-test.html concept
2. Add `mint_queue` table to Supabase (address, status, tx_hash, member_number)
3. Build Pi-side queue runner script
4. Wire member-only nav (localStorage session check on each protected page)
5. Mainnet CKBFS publish + 100 DOB mint once wallet funded

## Elitedesk screen lock fix
- Was locking instantly (idle-delay=0, lock-enabled=true)
- Fixed: lock disabled, idle-delay=1800 (30min blank, no lock)

## Session 2 — 2026-03-04 ~1:47–2:10 AM

### Supabase DB Setup — DONE
- Ran `supabase-setup.sql` in Supabase SQL editor — success, no errors
- Tables created: `mint_queue`, sequence `member_number_seq`
- RLS policies: anyone can insert/read; service role only for updates

### members.html — COMPLETE
- Full page live at wyltekindustries.com/members.html
- DOB preview image, spots claimed progress bar (0/100)
- JoyID connect → anonymous Supabase auth → mint_queue entry
- Shows member number + status pill (pending/minting/minted/failed)
- TX hash + explorer link shown once minted
- Sessions persisted in localStorage — returning users see their status
- Committed: b74241c to toastmanAu/wyltek-industries

### mint-queue-runner.js — WRITTEN
- File: /home/phill/ckb-dob-minter-script/mint-queue-runner.js
- Watches mint_queue for pending entries, mints DOBs via CCC + Spore
- Flags: --mainnet, --dry-run, --watch (60s poll)
- Needs env vars before mainnet run: MAINNET_CKBFS_TYPE_ID, MAINNET_CLUSTER_ID

### CKB Explorer Frontend PR — SUBMITTED
- Fork: toastmanAu/ckb-explorer-frontend
- Branch: feat/ckbfs-spore-rendering
- PR #2122: https://github.com/nervosnetwork/ckb-explorer-frontend/pull/2122
- Change: added `application/ckbfs` handler to `getSporeImg()` in src/utils/spore.ts
- Resolver: fetches CKBFS index cell by TypeID, decodes molecule data, reassembles multi-chunk file from witnesses, returns base64 data URL
- Uses isMainnet() for RPC endpoint selection
- Falls through to placeholder on any error

### Research Queue — 3 new tasks added
1. gameboy-hardware-wallet — R36S as hardware wallet, Batocera, button combo unlock
2. retroarch-core-blockchain — libretro API hooks for blockchain, Fiber micropayments in games
3. retro-achievements-ckb-bridge — rcheevos hooks → CKB events
- Budget handheld comparison scope broadened to all devices <$60 AUD
- Inspired by Neon's convo about Gameboy wallet + blockchain gaming
- Committed to kernel-workspace: 205c95c2

### Nav cleanup
- Removed Images, Roadmap, Node Updater, Node Finder from index.html nav
- Clean nav: Home, Devlog, BlackBox, CKB Sync, Flasher, Hardware, Light Client, CKB Stack, Members, GitHub

### What's left before going live
1. Fund mainnet minting wallet (~30,225 CKB)
2. Run mainnet CKBFS publish → get MAINNET_CKBFS_TYPE_ID
3. Create mainnet cluster → get MAINNET_CLUSTER_ID
4. Run mint-queue-runner.js --mainnet --watch
5. Open membership publicly / announce

---

## Afternoon session (continued) — 2026-03-04

### Membership System — FULLY LIVE ✅
- JoyID mainnet fix: `joyidAppURL: 'https://app.joy.id'` required (SDK defaults to testnet.joyid.dev — `network: 'mainnet'` alone doesn't work)
- Mainnet cluster created: 0x54ba3ee23016ab6e2e20792d8fd69057c62392ca1997b622147a5bd98979f4e8
- CKBFS TypeID (V2 mainnet): 0xbdf595ff79548ab67c5d852968ffa0d2491b28ea52687e736d52e661c9cdb76a
- Minting wallet funded with 13,000 CKB (tx: 0x23371470c1eeab4ce23212e00772f946c5972ba93f12d1f22597a2b458da5256)
- Member #1 (Phill) DOB minted: Spore 0x11365ac4d46ff9741fc34250c0159ba6844fa15cd7ded44a4011b4cb6d75e458
- Supabase mint_queue table: clean, member_number from live count (sequence-safe)
- Queue runner: /home/phill/ckb-dob-minter-script/mint-queue-runner.js --mainnet
- HEARTBEAT auto-mints new signups every ~30min, notifies via Telegram
- ~10,075 CKB remaining in minting wallet — enough for 99 more DOBs

### Website updates
- Nav: ✦ Join → ✦ Membership when logged in (localStorage check)
- Member-only gating: 10 pages behind member-guard.js (research, blog, flasher, ckb-sync, tests, images, themer, ckbfs-viewer, ckb-node-update, ckb-node-finder)
- Non-members see: Home, BlackBox, Hardware, Light Client, CKB Stack, Roadmap + ✦ Join
- Redirect-back flow: saves return URL in sessionStorage, redirects after JoyID login
- Research page rebuilt: expandable cards, lazy-load likes+comments per card
- "Toasts" not "Likes" — Kernel lobster-on-toaster image as the button, orange glow when active
- Supabase tables needed: research_likes, research_comments (SQL run by Phill in dashboard ✅)
- 100 founding members (was 50 — updated)
- All changes committed + pushed to GitHub

### Research crawler
- Notification fix: added send_telegram() call to research-crawl.py — was silently completing with no alerts
- All 24 prior tasks DONE, queue exhausted
- stack-gap-analysis-2 synthesis run: key findings — Fiber channel needs 99+ CKB to N100, Wyltek agent needs HF token, most blockers are execution not knowledge gaps
- No new tasks auto-queued (Gemini judged stack knowledge sufficient)

### Site stats / misc
- auth-test.html identified as obsolete (replaced by members.html)
- members.html: first 100 members, member_number assigned from live DB count
- ckb-dob-minter-script: added @supabase/supabase-js dep, CKBFS V2 pinned, constructor fixed

---

## Session — afternoon/evening 2026-03-04 (continued)

### @wyltek/ckbfs-browser SDK (NEW REPO)
- Created `/home/phill/workspace/ckbfs-browser-sdk/` — browser-native CKBFS V2 publisher
- Published to GitHub: https://github.com/toastmanAu/ckbfs-browser
- Package: `@wyltek/ckbfs-browser` — peer dep only on `@ckb-ccc/core`
- Key design: caller passes `ccc` module in (avoids peer dep bundling issues in Vite)
- Files: `src/constants.js`, `src/molecule.js`, `src/typeid.js`, `src/publisher.js`, `src/resolver.js`, `src/index.js`
- `publishCKBFS({ signer, ccc, content, contentType, filename, mainnet, onProgress })` → `{ txHash, typeId, uri, capacityCkb }`
- `estimateCKBFSCost(bytes)` → cost breakdown without needing a signer
- `resolveCKBFS(typeId, network)` → `{ fileBytes, contentType, filename }`

### CKBFS V2 Constants — Fully Verified
- `CKBFS_CODE_HASH` = `0x31e6376287d223b8c0410d562fb422f04d1d617b2947596a14c3d2efb7218d3a` (same mainnet + testnet)
- `CONTRACT.mainnet.txHash` = `0xfab07962ed7178ed88d450774e2a6ecd50bae856bdb9b692980be8c5147d1bfa` (dep_group)
- `CONTRACT.testnet.txHash` = `0x469af0d961dcaaedd872968a9388b546717a6ccfa47b3165b3f9c981e9d66aaa` (dep_group)
- Both verified against @ckbfs/api SDK constants + real mainnet tx `0xd824b272...`
- `VERIFICATION.md` added to repo with full breakdown
- Wire format: `"CKBFS\x00"` prefix + raw chunk bytes; molecule version field = 4 (not 2)

### DOB Minter — CKBFS upload wired up
- `src/lib/storage/index.js` `uploadCKBFS()` stub replaced with real implementation
- Dynamically imports `@wyltek/ckbfs-browser` + `@ckb-ccc/core`
- Installed package locally: `npm install /home/phill/workspace/ckbfs-browser-sdk`
- Build passes clean (`✓ built in 8.36s`)
- Pushed: https://github.com/toastmanAu/ckb-dob-minter commit `8f1d45d`

### Supabase profiles table — CREATED
- SQL run in Supabase dashboard, no rows returned (success)
- Table: `profiles` (ckb_address PK, display_name, bio, location, website, building, avatar_url, skip_banner bool, updated_at)
- RLS: public read, anon/member insert + update

### Address privacy
- Created `/home/phill/workspace/wyltek-industries-site/js/addr-format.js`
  - `shortAddr(addr)` → `ckb1qzda…sp8phrw` (8 + … + 6 chars)
  - `addrHtml(addr)` → truncated + ⧉ copy-to-clipboard button
  - Injects shared CSS for `.addr-display`, `.addr-short`, `.addr-copy-btn`
- Updated: members.html (wallet row), profile.html (ckbAddressDisplay + copy btn), research.html (already correct)
- Full address never rendered in DOM visible to other users
- Pushed: commit `4e407c7`

### Profile avatar upload — BLOCKED by Supabase RLS
- Error: "new row violates row-level security policy" on storage.objects
- Bucket `avatars` exists, is public, 3MB limit, JPEG/PNG/WebP allowed
- Fix: needs storage RLS policies for anon/authenticated on `storage.objects`
- SQL to run:
  ```sql
  CREATE POLICY "allow avatar uploads" ON storage.objects FOR INSERT TO anon, authenticated WITH CHECK (bucket_id = 'avatars');
  CREATE POLICY "allow avatar reads"   ON storage.objects FOR SELECT TO anon, authenticated USING (bucket_id = 'avatars');
  CREATE POLICY "allow avatar updates" ON storage.objects FOR UPDATE TO anon, authenticated USING (bucket_id = 'avatars');
  ```
- Status: waiting on Phill to run SQL — everything else on profile page working


---

## Evening session — 2026-03-04 ~21:30–22:00 ACST

### CKBFS Browser SDK — End-to-End Working ✅
- Full CKBFS V2 mint flow confirmed working in browser with JoyID on mainnet
- Root cause of "insufficient balance": JoyID validates tx client-side including witness size; fee rate of 1000 shannons/KB was too low for 30KB+ witness chunks. Fixed: bumped to 3000n + pre-reserve `chunks.length × 100000n` shannons before `completeInputsByCapacity`
- Root cause of viewer not rendering: `storageMode` was read from current React state at render time, but `clearFiles()` runs after mint. Fixed: bake `storageMode` into result object at mint time in `minter.js`
- Debug approach: added on-screen `🔍 debug info` collapsible panel (no DevTools needed on mobile) — confirmed working, stripped after fix verified
- SDK repo: https://github.com/toastmanAu/ckbfs-browser — latest commit f7d0c6a (cleanup after debug)
- Minter repo: https://github.com/toastmanAu/ckb-dob-minter — latest commit ae8a040

### Research Crawler — All Tasks Complete
- All 33 tasks DONE including stack-gap-analysis-3 synthesis
- Daily comment digest script: `/home/phill/.openclaw/workspace/scripts/review-research-comments.js`
- Cron: `0 9 * * *` — runs daily at 9am ACST, logs to `/tmp/research-comments.log`
- First live data: 1 toast already on `bitchat-ble-transport` task

### Blog Posts Published
- "We built the thing that didn't exist: browser-native CKBFS publishing" (2026-03-04)
  - Documents all CCC API gotchas for posterity
- "Membership is live — first 100 get a Founding Member DOB" (2026-03-04)

### AGENTS.md Updated
- Added "Browser App Debugging Rules" section — always console.log first, log at boundaries, use [ComponentName] prefixes, add on-screen debug panel for mobile

### Phill's next action
- Final test of CKBFS mint without debug code → record video for Nervos Nation

---

## CKBFS Mint Debug Session — 2026-03-04 ~22:30 ACST

### Root cause of "Need 71223 extra CKB" error
- Error = `completeInputsByCapacity` throwing `ErrorTransactionInsufficientCapacity`
- 71,223 CKB + 12,673 CKB balance = ~83,896 CKB = exactly 81.8KB file bytes treated as CKB
- Extensive debugging: manual capacity formula was correct (~202 CKB), `capacity=0x0` auto-calc also correct
- **Real fix**: switched from homebrew V2 tx builder to official `@ckbfs/api` `createPublishV3Transaction`
- Also upgraded to **CKBFS V3** (contract `20250821.4ee6689bf7ec`, code_hash `0xb5d13ffe0547c78021c01fe24dce2e959a1ed8edbca3cb93dd2e9f57fb56d695`)
- V3 dep group mainnet txHash: `0x03deba7f8206c81981d6f6a2d61b67dde75b4df91cbcfaf2e2fb041ba50c4719`

### `@wyltek/ckbfs-browser` SDK changes
- `publisher.js` now just wraps `@ckbfs/api`'s `createPublishV3Transaction` — no custom tx building
- `ckbfs` module now required as param: `publishCKBFS({ signer, ccc, ckbfs, content, ... })`
- SDK commit: `d972378`

### DOB Minter fixes
- Storage layer (`src/lib/storage/index.js`) now imports and passes `@ckbfs/api` to `publishCKBFS`
- `result.uri` → `` `ckbfs://${result.typeId}` `` (V3 doesn't return uri directly)
- Network + storageMode now persist to localStorage (survive tab swaps)
- Switched from `vite dev` to production `serve dist` — **no more auto-refresh wiping state**
  - Service: `/home/phill/.config/systemd/user/ckb-dob-minter.service`
  - Now runs: `serve dist -l 5173` (static production build)
  - Deploy flow going forward: `npm run build` → `systemctl --user restart ckb-dob-minter`
- Minter commit: `46c9d14`

### State reset issue resolved
- Was running `vite dev` with HMR — every code change auto-refreshed browser, wiping all form state
- Now production static build — no HMR, no surprise refreshes
- Files still reset on tab swap (unavoidable — File objects can't be serialized to localStorage)

### CKBFS V3 vs V2 key differences
- V3: witness-based storage, Adler32 checksum in cell data, no backlink arrays
- V3 outputData: `{ index, checksum, contentType, filename }` (smaller than V2)
- V3 more affordable due to smaller cell data
- SDK `DEFAULT_VERSION` is already V3 as of `@ckbfs/api` current version

### Status at end of session
- Production build live at http://192.168.68.82:5173
- Awaiting Phill's test of CKBFS mint with 81.8KB image on V3
