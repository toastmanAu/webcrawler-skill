# 2026-03-15 — Morning + Afternoon Session

## Morning Fixes (Heartbeat ~8:16 AM)

### Stratum Proxy
- Was down on morning check → restarted via `bash /home/phill/ckb-stratum-proxy/start.sh` ✅

### Backup Git Push
- `git remote` was missing from workspace repo
- `vscode.deb` (107MB) was committed to history → rewrote history with `git filter-branch`, added to `.gitignore`, force-pushed ✅
- Workspace remote: `https://github.com/toastmanAu/kernel-workspace.git`

### CKB Dashboard Host Fix
- Dashboard was hardcoded to `192.168.68.87` (ckbnode WiFi) — but WiFi unreachable from Pi
- Fixed to `192.168.68.105` (ckbnode ethernet) in `server.js`
- `ssh ckbnode` alias → `192.168.68.105` (ethernet), `ckbnode-wifi` → `192.168.68.87`

## Research — Extended Game RAM Maps

Scraped and documented RAM addresses for FiberQuest extended game catalog:

### F-Zero (SNES) — sneslab.net ✅ CONFIRMED
- `0x00C9` (2B) — Player machine power (energy bar)
- `0x0059` — Spare machines (extra lives)
- `0x1150` — Race position/rank (0x00 = 1st)
- `0x00C0/C1/C2` — Race timer (min/sec/csec)
- `0x0053` — Current race number
- `0x0090` — League (0=Knight, 1=Queen, 2=King)

### Killer Instinct (SNES) — gamehacking.org
- `0x0D28` + `0x0D2A` — P2 health (max 0x78)
- P1 health NOT publicly mapped — needs in-emulator search (~`0x0D26`?)

### Super Mario World (SNES)
- `0x0DBF` — Coins (8-bit, confirmed)
- Lives address: not found (DataCrystal is a stub)

### Zelda: A Link to the Past (SNES)
- `0xF36C` — Health capacity (SRAM, 0x08/heart)
- `0xF36D` — Current health (SRAM)
- `0xF028` — Rupees (SRAM ~7EF028)
- ⚠️ These are SRAM not live WRAM — test with `READ_CORE_MEMORY 0xF028 2` in RetroArch snes9x

### Super Bomberman (SNES)
- ~`0x0434` lives (unverified, estimated from SB4 series)
- GitHub disassembly exists (LIJI32/superbomberman) but RAM not fully mapped

### Super Mario Kart, Streets of Rage 2
- Not found in this session

Findings saved: `research/findings/fiberquest-ram-maps-extended.md` → committed + pushed ✅

## Research Browser Tool (lib/research-browser.js)

- Rebuilt to delegate scraping to NucBox (192.168.68.79) via SSH + temp file
- Puppeteer installed on NucBox (`~/node_modules/puppeteer`) ✅
- Chrome launches correctly on NucBox (x86, not ARM)
- Pi ARM Chromium too slow (~30s launch = timeout) → don't use locally
- `NODE_PATH=~/node_modules` required when running scripts from `/tmp` on NucBox
- Works on open sites; Cloudflare-protected sites (RetroAchievements, gamehacking.org) block headless Chrome regardless
- Committed: `feat: research-browser delegates scraping to NucBox via SSH+tempfile`

## FiberQuest Pi Power/Network Issues

### Problem chain:
1. Pi 5 PSU was insufficient → browned out under Rust compile load (WiFi dropped first)
2. New 5.1V 5A PSU installed → throttled=0x0 (clean) ✅
3. Still dropped mid-compile → WiFi instability under heavy CPU load (known Pi 5 issue)
4. Fix: Ethernet cable added, WiFi disabled (`sudo ip link set wlan0 down`)

### FiberQuest Pi new config:
- **Ethernet IP: 192.168.68.84** ← use this going forward
- **WiFi IP: 192.168.68.65** ← disabled
- SSH alias `fiberquest` updated to `192.168.68.84` in `~/.ssh/config`
- Fiber build running (3rd attempt) — cargo building, Pi staying online

### Fiber build status:
- Source cached at `~/.fiber-build-cache/fiber/` (1.4GB, v0.7.1)
- `cargo build --release` running as of ~14:52 ACST
- Install script: `bash /tmp/fiber-install.sh` (note: /tmp gets wiped on reboot, re-download if needed)
- Download: `curl -sf https://raw.githubusercontent.com/toastmanAu/ckb-access/main/fiber/install.sh -o /tmp/fiber-install.sh`
- Log: `~/fiber-build.log` on fiberquest Pi
- ETA: 15-30 min ARM compile time

## Kernel Dashboard Rebuild

### What changed:
- `index.html` replaced WyDash public module manager with private personal ops dashboard
- Sidebar: collapsible icon-only by default (48px), click 🐧 logo to expand to full labels
- State saved in localStorage
- Sections: Overview (Home, Services), CKB (Node, Mining, Fiber), Tools (X Analytics, Projects, DOB Burner, Research), External links

### Bug fixes applied:
- All `localhost` URLs → `192.168.68.82` (Pi LAN IP) — was breaking mobile view
- `/api/node` (nonexistent) → `/history` (real CKB dashboard endpoint)
- Stratum check: `/health` → `/` (stratum has no /health endpoint)
- Stratum fetch: `/proxy-stats` → `/` 

### CKB Dashboard data shape (from /history):
- `tipHeight` — current block number
- `avgBlockTimeSec` — avg block time string
- `networkHashrate` — string e.g. "131.93 PH/s"
- `blocks[]` — array of {number, timestamp, txCount, blockTime}

## X Analytics Dashboard

### Status:
- Bearer Token saved: `secrets/x-api.env` (X_BEARER_TOKEN)
- API returns HTTP 402 `CreditsDepleted` — app needs paid API tier ($100/month Basic)
- Decision: use CSV export instead (free, better data, 28-day history)

### What was built:
- `kernel-dash/x-analytics.html` rebuilt for CSV workflow
  - Drag+drop or file picker CSV upload
  - Auto-load from `/x-analytics-latest.csv` served by kernel-dash
  - Charts: daily impressions (bar), engagement breakdown (doughnut), best hours (bar)
  - Top tweets by engagement score (likes×1 + replies×2 + reposts×3 + quotes×2)
  - Algorithm tips panel (personalised based on data)

### Auto-downloader:
- Script: `~/x-analytics-downloader.js` on NucBox
- Setup cmd: `ssh phill@192.168.68.79 && node ~/x-analytics-downloader.js --setup`
- Credentials stored in: `~/x-analytics-data/config.json` (chmod 600, NucBox only)
- Crons on Pi (6am daily):
  - `0 6 * * *` → SSH to NucBox, run downloader
  - `5 6 * * *` → rsync `latest.csv` from NucBox to `kernel-dash/x-analytics-latest.csv`

### CSV parser fixes:
- BOM stripping, Windows CRLF handling, quote stripping from values
- Date parsing: tries multiple formats, falls back to show all rows if no dates parse
- Debug logging added to console on CSV load

### CSV column names (X Analytics export):
- `tweet id`, `tweet permalink`, `tweet text`, `time`, `impressions`, `engagements`
- `engagement rate`, `retweets`, `replies`, `likes`, `user profile clicks`
- `url clicks`, `hashtag clicks`, `detail expands`, etc.

## Notes / Lessons

- Pi 5 WiFi is unreliable under heavy CPU load — always use ethernet for compile jobs
- `/tmp` is volatile on reboot — don't rely on scripts stored there
- `localhost` in browser JS = the viewer's device, not the server — always use LAN IP for status checks
- CKB dashboard has no `/api/node` — use `/history` for block data
- `throttled=0x0` from `vcgencmd get_throttled` = no power issues (requires sudo on fiberquest)
- X API now credits-based even on paid tiers; CSV export is better for analytics anyway
- NucBox Puppeteer path: `NODE_PATH=~/node_modules node script.js` (modules at `~/node_modules/`)

## FiberQuest Pi — Fiber Node Setup Complete (2026-03-15 ~15:35 ACST)

### Key details
- **Wallet address (mainnet):** `ckb1qzda0cr08m85hc8jlnfp3zer7xulejywt49kt2rr0vthywaa50xwsq27zrhwxc5ppa29ue522vk36m0zhdq8g4q2kfh02`
- **Lock arg:** `0x5e10eee362810f545e668a532d1d6de2bb407454`
- **Pubkey:** `039a191f58d0263f1e40f3301657b53a8da8e136dfcdc672ceaa1458ea5c024b6f`
- **Key password:** `fiberquest-fiber-2026`
- **Private key path:** `~/.fiber-mainnet/ckb/key` (binary encrypted, fnn manages)
- **Config:** `~/.fiber-mainnet/data/config.yml`
- **CKB RPC:** `http://192.168.68.105:8114/` (ckbnode ethernet)
- **P2P port:** 8228
- **RPC port:** 127.0.0.1:8227
- **Dashboard port:** 8229

### Services
- `systemctl --user start/stop/restart fiber` — fnn node
- `systemctl --user start/stop/restart fiber-dash` — dashboard on :8229
- Logs: `~/.fiber-mainnet/data/fiber.log`

### Next steps
- Send **at least 162 CKB** to wallet address above to fund the node
- Open a channel to ckbnode (`026a9dd1bae2e7c9ee5acaf7ad8e2e7a89fcca183740f9c9f761e402ad1da70da0` @ port 8228) once funded

### Issues resolved
- Config had duplicate rpc/ckb/services sections → rewrote clean
- `--dir ~/.fiber-mainnet` flag required (installer systemd uses it, fnn can't find files without it)
- `ckb/key` gets encrypted in-place by fnn on first start (77 bytes binary)
- `data/key` is NOT auto-created — fnn uses `private_key_path` as the encrypted key file
- Dashboard service had `Restart=on-failure` in wrong part of ExecStart → fixed
- ckbnode was using WiFi IP (192.168.68.87, unreachable from Pi); updated to ethernet IP (192.168.68.105)

## Evening Session — OPi5+ Telegram Bot Migration & Testnet Snapshot (19:30–20:30 ACDT)

### Telegram Bot Migration (N100 → OPi5+)

**Goal:** Move @Wyltek_n100_bot from NucBox to OPi5+, maintain local inference only (no paid APIs).

**Process:**
1. Disabled Telegram plugin on N100 (openclaw.json `channels.telegram.enabled = false`, `plugins.entries.telegram.enabled = false`)
2. Added Telegram config to OPi5+ with bot token `8757576179:AAEUSgouk6l-05YG6WmZjOeAUR6XQ0kKgG8`
3. Copied credential files from N100: `telegram-default-allowFrom.json`, `telegram-pairing.json`, `update-offset-default.json`
4. Set up rclone on OPi5+ (for future snapshot uploads to Backblaze R2)
5. Copied SSH key from NucBox for passwordless ckbnode access

**Model Provider Evolution:**
- **Initial:** HuggingFace API (API key missing → failed with "fetch failed")
- **Attempt 1:** Tried `ryzen/qwen2.5:14b` (Ollama on NucBox 192.168.68.79:11434) — but model took 3+ minutes to load initially, causing timeout errors at 19:35–19:53
- **Temp fix:** Switched to `ckbdev/claude-sonnet-4-6` (CKBDev shared builders API) — Phill said no, wants local inference only
- **Final:** Reverted to `ryzen/qwen2.5:14b` but increased `requestTimeout: 120000` (120 seconds) in provider config

**Key findings:**
- Ollama warm response time: ~3.4 seconds (very fast once loaded)
- Previous "Ollama API stream ended without a final response" was due to client timeout too short (default likely 30s?), not API speed
- Model context truncation warning on Ollama logs: "truncating input prompt limit=2048 prompt=18079" — Ollama runner configured with n_ctx_per_seq=2048, not full 32768. This caused large history to be sent each turn.
- OPi5+ OpenClaw version: downgraded from 2026.3.13 to 2026.2.22-2 (2026.3.13 had plugin incompatibility)

**Status:** Gateway listening, Telegram plugin loaded, bot ready for test. Awaiting Phill's DM test.

**Fallback option:** If qwen2.5:14b proves unreliable, install Ollama directly on OPi5+ with qwen2.5:3b model (smaller, faster).

### Testnet Snapshot Download Issue & Resolution

**Problem chain:**
1. Initial download via wget: file grew to 141GB but stalled
2. File was corrupted: `7z t snapshot.7z` showed "Headers Error" and "There are data after the end of archive"
3. Local file was **483MB larger** than server file (150.3GB local vs 149.8GB server)
4. `wget -c` failed with 416 Range Not Satisfiable (server doesn't support resume or file was corrupted in place)

**Root cause:** Previous wget/resume attempt wrote junk bytes beyond server file size, corrupting archive.

**Solution:** 
- Deleted corrupted file
- Started clean re-download at 20:21 ACDT
- Server file size: **139.6 GB** (149,848,944,471 bytes)
- ETA: ~23:00 ACDT (at ~0.7GB/min rate observed from fresh download)
- Disk space: 313GB free, sufficient for extraction

**Note:** Earlier we reported "218GB testnet snapshot" — that appears to be uncompressed size. Compressed is 139.6GB.

### Key Decisions Made

1. **Local inference only on OPi5+** — no Claude, no paid APIs (per Phill's preference)
2. **Stratum proxy IP:** Hardcoded to 192.168.68.105 (ckbnode ethernet, not WiFi .87)
3. **Revert from Claude Sonnet to Ollama locally** — confirmed qwen2.5:14b works with increased timeout

### XFCE4 Desktop Setup — Complete ✅ (21:17 ACDT)

**Final fix:** Installed `dbus-x11` (dbus-launch was missing). Set up systemd service `xfce-auto.service` to auto-start XFCE on boot.

**Outcome:**
- Desktop auto-boots on system restart
- Auto-login via systemd service (runs as phill user, starts X/XFCE automatically)
- First reboot confirmed working — Phill saw XFCE4 desktop on HDMI ✅

**Key packages installed:**
- xfce4, xfce4-goodies, lightdm, lightdm-gtk-greeter (initial, LightDM had issues)
- xorg, xserver-xorg-core, xserver-xorg-video-fbdev (needed for ARM)
- xinit (for startx)
- dbus, dbus-x11 (for XFCE session management)

**Workaround notes:**
- LightDM startup failed (parse_vt_settings permission issues) — disabled in favor of systemd service
- Xorg required root privileges (tty0 access) — solved via `sudo startx`
- systemd service approach (xfce-auto.service) cleaner than trying to fix LightDM

### Pending

- Bot test confirmation from Phill (DM to @Wyltek_n100_bot)
- Snapshot re-download completion (~22:30 ACDT, at 38.4GB/139.6GB as of 21:15)
- Snapshot extraction to `/home/phill/ckb-testnet/data/`
- Testnet CKB node startup
- Dual-chain snapshot script adaptation (mainnet from ckbnode, testnet from OPi5+ local)
- Biscuit auth decision for fiber dashboard
