# Memory — 2026-03-07

## JoyID Cross-Browser Fix — SHIPPED ✅

### What we built
- `/js/joyid-connector.js` — custom JoyID connector wrapping `@joyid/common` internals directly
- Uses `authWithRedirect()` + `authCallback()` from `@joyid/common` (existed but not re-exported by `@joyid/ckb`)
- Browser detection via `typeof navigator.brave !== 'undefined'` (synchronous, reliable — `isBrave()` is async/Promise)
- Firefox desktop: show clear "use Brave/Chrome/phone" message — CTAP2 hybrid not supported

### Final browser scorecard
- Chrome ✅ popup, 1 shot
- Brave ✅ redirect flow, 1 shot
- Chromium ✅ redirect flow, 1 shot
- Safari iOS ✅ 1 tap Face ID
- Firefox ⚠ clear message + phone option (CTAP2 hybrid not supported by Firefox)

### Key technical insight
- `navigator.brave.isBrave` is a **Promise** (async) — calling without `await` returns truthy function object
- Must use `typeof navigator.brave !== 'undefined'` for synchronous Brave detection
- `authWithRedirect()` navigates whole tab to `app.joy.id` — no WebSocket relay, no QR scanning
- Result returns as `?_data_=` + `?joyid-redirect=true` params on `redirectURL`
- `authCallback()` parses result from current URL

### Commits
- `7517189` — initial custom connector + joyid-callback.html rewrite
- `37dad7d` — fix: loadJoyIDConnector definition dropped in edit
- `362ad56` — fix: Brave detection async issue
- `fa27f17` — fix: Firefox desktop clear error message

### GitHub issue filed
- https://github.com/nervina-labs/joyid-sdk-js/issues/58
- Requested: export `authWithRedirect`, `authCallback`, `isRedirectFromJoyID` from `@joyid/ckb`
- One-line fix on their end

## Planned: Pinchy + Toastman Loader Animation
- Replace all spinners on wyltekindustries.com with branded animation
- Pinchy (lobster) walks in from left, Toastman (toaster) pops up from right
- Meet in middle → handshake/fist bump
- "Connecting to JoyID…" fades in underneath
- On success: confetti or Pinchy victory claw raise
- Reusable: members connect, DOB minting, lounge messages
- Module: `/js/wyltek-loader.js`
- Status: PLANNED — do after JoyID work settled

## Cloudflare API Token — ROLLED
- New token: DBY6g50iWKIfscJW6ivhKbkEF6RbJN7K1L3zWLAI
- Stored at: /home/phill/.cloudflare-token (chmod 600)
- Workers redeployed with new token: wyltek-lounge + wyltek-bug-reporter
- Old token revoked by Phill

## Tomorrow's Targets — 2026-03-08

### Priority 1: T-QT C6 CkbLightC6 firmware
- Write `examples/CkbLightC6/` in ckb-light-esp repo
- Full light client sync (WiFi6)
- Touch UI via CST816T: SDA=21, SCL=22, INT=7, RST=23, addr=0x15
- GM861S QR scanner wired up (barcode scanner for receiving payments)
- Display: block height, sync status, wallet address QR
- Board plugged in at /dev/ttyACM0 on Pi 5 (need Pi 5 power cycled first)
- Flash from EliteDesk if Pi 5 still down

### Priority 2: LoRa node bridge IRL test
- Real-world range test between LoRa nodes
- Confirm CKB light client headers relaying over LoRa
- Note: which boards/nodes are we testing with? (confirm with Phill in morning)

### Also if time:
- Zero 3 display wiring check (DC/RST swap — white screen debug)
- Pi 5 power cycle + check Fiber build status

### Pre-conditions:
- Pi 5 needs power cycle before T-QT C6 work (device at /dev/ttyACM0)
- OR flash from EliteDesk directly (has ESP-IDF + ckb-light-esp already)

## SenseCAP M1 Repurpose — Research Task
- Currently plugged in mining Helium (rewards suck, Phill wants to repurpose)
- Hardware: Raspberry Pi 4 inside, SX1302 LoRa concentrator HAT (8-channel)
- Plan: swap SD → Raspbian Lite → ckb-light-client container + LoRa bridge
- SX1302 is a concentrator (not single channel) — can receive from multiple LoRa nodes simultaneously
- RAM size unknown — check before planning Fiber node co-location
- Research overnight: SX1302 HAL on Pi4, Docker container for ckb-light-client ARM64, SenseCAP M1 ECC key backup, known SD swap guides

## Late Night Session — 2026-03-07 ~3am ACST

### Trading Bot
- Bot was dead since Mar 1 due to DNS failure (socket.gaierror) — restarted on N100 (pid 535753)
- Strategy: RSI(14) + EMA50/200 on BTCUSDT 4h, paper mode
- Current signal: 🔴 STAY OUT — BTC $68,514, below EMA-200 ($70,453) and EMA-50 ($69,052), RSI 36.6

### CKB Chess
- Theoretically buildable now with Fiber + JoyID + chess.js
- Missing piece: trustless state verification (trusted arbiter fine for MVP/demo)
- FiberQuest IS the same plumbing — retro game instead of chess board

### T-QT C6 + T-Deck LoRa IRL test — Tomorrow's plan
- CkbLightC6 example written: examples/CkbLightC6/src/CkbLightC6.ino
  - ST7789 128x128, CST816T touch (tap to cycle screens), GM861S scanner on UART1 GPIO8/9
  - LIGHT_PROFILE_MINIMAL, WiFi6 sync, 3 screens: status / wallet / scanner
- CkbLightTDeck example written: examples/CkbLightTDeck/src/CkbLightTDeck.ino
  - ST7789 320x240, SX1262 LoRa, BB Q10 keyboard, GM861S scanner
  - LIGHT_PROFILE_MINIMAL + LIGHT_WITH_LORA
  - Broadcasts verified CKB block headers over LoRa every 30s (CKBHDR:<block>)
  - Receives + cross-verifies peer headers — two-way trustless verification
  - Keys: S=status, Q=scan, L=LoRa, N=node
- Both committed: b014380 to ckb-light-esp repo
- T-Deck is the field test unit (has SX1262 LoRa + keyboard + proper display)
- Two light clients = neither trusts the other, both verify independently against CKB chain

### SenseCAP M1 — TWO units on LAN
- .81 = WiFi unit (192.168.68.81)
- .85 = Ethernet unit (192.168.68.85) ← THIS is the main one (wired preferred)
- Both running SenseCAP local console (Werkzeug/Python, port 80)
- Login password = serial number printed on back of unit
- Helium rewards suck — Phill wants to repurpose for CKB
- Plan: check Docker access via local console first (no SD swap needed if Docker accessible)
- Hardware inside: Raspberry Pi 4, SX1302 LoRa concentrator (8-channel, massive upgrade over SX1262)
- Community projects: hereismeaw/SenseCAPM1-Chirpstack (TTN gateway — exact path we'd follow)
- CRITICAL: copy config.json off SD before any swap — contains Helium identity
- Research task queued in research/queue.md: sensecap-m1-ckb-repurpose (HIGH priority)

### driveThree
- Shut down tonight (~3:22am ACST) via `sudo shutdown now` — confirmed clean disconnect

### Tomorrow (2026-03-08) confirmed targets
1. T-QT C6 CkbLightC6 firmware — flash + verify sync + touch + GM861S
2. T-Deck CkbLightTDeck firmware — flash + LoRa IRL range test
3. SenseCAP M1 (.85) — get serial, log into local console, check Docker access
4. Zero 3 display wiring check (dalight)
5. Pi 5 power cycle
