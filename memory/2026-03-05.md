# Memory — 2026-03-05

## DOB Minter UI Fixes (late night 2026-03-04 → 03-05)

### Screen recording analysis
- Phill sent screen recording of minter on mobile
- JoyID was connecting to testnet.joyid.dev (was on testnet network)
- Wallet address was wrapping across ~13 lines (word-break: break-all bug)

### Fixes deployed
- `WalletPanel.jsx`: address now truncated `first12…last8` with full address in `title`
- `styles.css`: `.wallet-address` → `white-space: nowrap; overflow: hidden; text-overflow: ellipsis`
- `ckbfs-resolver.js`: fixed null crash on V3 cells — `meta.indexes[0]` and `meta.indexes.length` were called when `indexes` is null for V3. Fixed to use `meta.index ?? 1` and `chunks.length` fallbacks.
- `NodePanel.jsx`: full redesign — URL on its own line below status, "Connecting…" feedback while probing, URL input full-width above buttons, Connect disabled until URL typed, pre-fills current URL on "change"
- Minter commits: `4ab8f73` (wallet truncate), `477e011` (resolver null fix), `01b2ffa` (node panel), `c691a1c` (testnet light client scanner)

## driveThree (192.168.68.88)
- GNOME auto-suspend was causing daily reboots — disabled via gsettings + logind.conf
- qwen2.5:14b pull completed (~9GB, RTX 3060 Ti) — tested working, returns inference
- Ollama API: http://192.168.68.88:11434

## CKB Testnet Light Client (N100)
- New instance: `~/ckb-light-testnet/` — separate from mainnet at `~/ckb-light-client/`
- RPC: `http://192.168.68.91:9001` (LAN accessible)
- P2P: port 8119
- Chain confirmed testnet (genesis 0x10639e...)
- Auto-discovered by minter (port 9001 added to LAN scan)

## OPi3B Testnet Full Node (abandoned)
- Started CKB full node on opi3b-armbian at port 8116 but decided against full sync (days)
- Light client on N100 is the better approach
- Node process still running on opi3b — may want to kill it

## Minter Node Finder
- `LAN_PORTS` now: `[8114, 28114, 9001]`
- localhost probes include port 9001

## Key Context
- For testnet CKBFS minting: switch minter to Testnet, hard refresh → auto-discovers N100:9001
- For mainnet minting: use ckbnode at 192.168.68.87:8114 (full node with indexer)
- driveThree SSH: `ssh phill@192.168.68.88` (no alias in ~/.ssh/config yet)

## NucBox K8 Plus (Ryzen 7 8845HS)
- IP: 192.168.68.79 · hostname: phill-NucBox-K8-Plus · user: phill
- SSH: ssh phill@192.168.68.79 (NOPASSWD sudo now configured)
- Ryzen 7 8845HS, Ubuntu (kernel 6.17.0)
- Auto-suspend masked (sleep/suspend/hibernate targets → /dev/null)
- logind drop-in: /etc/systemd/logind.conf.d/nosuspend.conf

## NucBox K8 Plus — Inference Details (corrected)
- AMD Ryzen 7 8845HS · Radeon 780M iGPU (Phoenix3) · 32GB RAM
- This is the PERMANENT local inference machine (always on)
- driveThree (192.168.68.88) is the secondary inference node (RTX 3060 Ti, on-demand)
- Ollama API: http://192.168.68.79:11434
- Models loaded: qwen2.5:14b, minicpm-v (vision!), codellama:13b, gemma3:12b, deepseek-r1:14b, qwen2.5-coder:14b, llama3.1:8b + more
- minicpm-v = vision model (image analysis) — this is what the image tool tries to use as "ryzen/minicpm-v:latest"

## Session 2026-03-05 01:00–01:16 ACST

### ckbfs-viewer.html — DOB Inventory Viewer (complete rewrite)
- Old: single TypeID manual lookup tool (396 lines)
- New: full inventory grid + detail panel + manual lookup fallback (551 lines)
- Features:
  - Auto-loads all Spore cells for connected wallet via `get_cells` RPC (lock script filter)
  - Mainnet / Testnet toggle
  - Lazy thumbnail loading per card (inline image → CKBFS resolve fallback)
  - Slide-in detail panel (right side desktop, bottom sheet mobile)
  - Detail: image preview, metadata table, Explorer link, Copy ID, "View in Minter" button
  - bech32m address decode to extract lock args (handles JoyID 22-byte args + secp256k1 20-byte)
- Commit: e4e30ba

### DOB Minter — moved to /mint/ subfolder on main site
- Was: separate subdomain mint.wyltekindustries.com (GitHub Pages gh-pages branch)
- Now: wyltekindustries.com/mint/ (subfolder in site repo, built with base: '/mint/')
- All links on site updated from mint.wyltekindustries.com → /mint/
- Phill added Cloudflare DNS CNAME record (mint → toastmanau.github.io) but then decided subdomain not needed
- DNS CNAME can be deleted now (or leave it, will redirect to same place via GitHub Pages CNAME file)
- Commit: 86075be (site), b88ecf1 (minter)
- Deploy flow: build with base '/mint/' → copy dist/ → wyltek-industries-site/mint/

### Members page — Member Tools grid
- Added card grid below the DOB queue section: Minter, Inventory, Research, Profile
- Footer quick-links updated to match
- Commit: 2ffe370

### Mainnet caution banner
- Orange warning bar appears when on Mainnet: warns about permanent CKB locking
- "Testnet" link in banner is clickable — switches network toggle directly
- Commit: b88ecf1

### Burn mechanism (meltSpore via CCC)
- New BurnPanel.jsx component — uses @ckb-ccc/spore `meltSpore({ signer, id })`
- Header toggle: Mint | 🔥 Burn (burn button highlighted red when active)
- Two-step confirm: paste Spore ID → warning box shows → "Confirm Burn" or Cancel
- On success: shows CKB returned to wallet + TX explorer link
- Commit: f9d17ec (minter), 44fceba (site)

### Network audit (all clean)
- CCC client: ClientPublicTestnet/Mainnet driven by network toggle
- RPC: mainnet.ckbapp.dev vs testnet.ckbapp.dev — correct
- Explorer links: network-aware throughout
- CKBFS V3 constants: separate mainnet/testnet tx hashes
- JoyID: uses ccc.Provider which respects active client (no hardcoded joyidAppURL needed in minter)
- All cell-locking bugs from testing confirmed fixed: witnesses-after-inputs, fee-after-witnesses, shouldAddInputs:false

### Current deploy state
- wyltekindustries.com/mint/ — minter live (burn + mainnet caution)
- wyltekindustries.com/ckbfs-viewer.html — inventory viewer live
- wyltekindustries.com/members.html — member tools grid live
- Local dev: http://192.168.68.82:5173 (systemd ckb-dob-minter, production static serve)

---

## Session 2026-03-05 02:00–02:34 ACST

### FiberQuest — Console Hub Research
Phill proposed a new angle: ESP32-S3/P4 as a **central console hub** that emulates native console multiplayer link protocols so games play together "as intended" — no modifications, invisible payment layer underneath.

#### Key findings from 5 new research tasks:

**ESP32-P4 — confirmed ideal chip:**
- Dual-core RISC-V 400MHz (vs S3's 240MHz), 50+ GPIOs, 768KB SRAM + 32MB PSRAM support
- 8KB zero-wait TCM RAM + ETM (Event-Task Manager) — hardware-level microsecond timing without CPU
- USB OTG HS 480Mbps — connect modern USB controllers directly
- SPI x3, UART x3, RMT — maps well to console protocols
- P4 is significantly better than S3 for real-time protocol emulation

**Console link protocols:**
- Game Boy Link Cable: SPI-like synchronous serial, master/slave via clock, 8-bit bytes → ESP32 SPI peripheral maps well; **5V logic → needs TXS0108E level shifter**
- SNES controller: Shift register polling via LATCH/CLOCK/DATA → RMT peripheral ideal, ~120Hz
- NES Four Score: Shift register + strobe → RMT suitable
- Voltage: All these consoles are 5V; ESP32 is 3.3V — level shifting required on all lines
- N64 Joybus, GBA link, Genesis: not found in seeds — need manual research

**No prior art found:** Zero existing ESP32 projects doing SNES/Game Boy link emulation on GitHub searches. Genuinely unexplored territory.

**Recommended MVP demo:** One SNES with two controllers → ESP32-P4 reads both → button press triggers Fiber micropayment → display shows live balances. Games don't know anything changed.

**Recommended hackathon approach: Hybrid**
- RetroArch handles emulation (saves huge dev time)
- Small ESP32-P4 board reads real console controllers + integrates Fiber payments
- Outputs to RetroArch via USB HID
- Best judge impact per hour of development

**Post-hackathon commercial vision:** Universal retro controller adapter + invisible Web3 payments. Arcade operators, retro enthusiasts. Open hardware + protocol standard.

### FiberQuest Intel Portal — Feedback Loop Added
- Portal URL: http://100.115.197.42:9990 (Tailscale only)
- New: each research section has a 💬 Follow-up Questions box
  - Textarea + tag selector (Hardware/Protocol/Fiber/Game/Business/Urgent)
  - Submit queues a new research task immediately to queue.md
  - Pi SSHes to NucBox, fires crawler in background
  - Findings sync back every 20 min, page auto-rebuilds
- Server: `server.py` (Python HTTPServer + POST /feedback endpoint)
- Rebuild script: `rebuild.py` (regenerates full HTML with feedback items)
- Systemd service `fiberquest-intel` updated to use server.py
- NucBox idle crawler: cron `*/15 * * * *` → research-crawl.py → /tmp/fq-crawl.log
- NucBox findings sync back to Pi: cron `*/20 * * * *` → rsync findings dir
- Pi page rebuild: cron `*/20 * * * *` → rebuild.py
- Files: /home/phill/fiberquest-intel/{server.py, rebuild.py, feedback.json, index.html}

### Fiber vs CKBFS — Important Clarification
- Research crawler (Gemini) hallucinated "Fiber Archive Agent" concept — confused two things:
  1. `nervosnetwork/fiber-archive` = old abandoned GitHub repo (2021), "archived" = GitHub flag
  2. CKBFS = actual CKB cell-based file storage (what we use)
- **Fiber CANNOT store data** — it's a payment channel network (like Lightning)
  - Only channel open/close txs touch L1; everything between is off-chain message passing
  - No user data stored, no file storage capability
- **CKBFS stores data** — files chunked across CKB cells, fully on L1
- Correct mental model: Fiber = money pipe, CKBFS = storage layer, CKB L1 = anchoring
- FiberQuest architecture is correct: CKBFS stores game content, Fiber routes payments

### Research queue state (2026-03-05 02:34 ACST)
- 32 sections in Intel portal
- NucBox idle crawler now active (runs every 15min)
- Feedback loop operational — Phill can add follow-up questions from phone, NucBox crawls overnight
