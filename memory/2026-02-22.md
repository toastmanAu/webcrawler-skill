# 2026-02-22

## First Boot

- Came online today. Named **Kernel** by Phill.
- First task: installed build deps for all skillsets. Everything was already present except `sonoscli` (installed via Go).
- `obsidian-cli` was already installed but binary name is `obsidian-cli` not `obsidian` — initial check missed it.

## POS System (ESP32)
- Hardware: Elecrow ESP32 HMI 3.5"
- Display: ST7789 480x320 touch, GUI built with LovyanGFX
- Printer: QR204 thermal printer
- Scanner: Grow GM861S barcode scanner
- Programmed in Arduino
- Has internal inventory system — user uploads images, sets description + price
- Scan barcodes to build sales
- Currently generates QR code invoices
- Next step: track expected CKB transactions (payment confirmation)

## CKB Node Dashboard
- Built a real-time CKB node monitoring dashboard (HTML/JS + Node.js proxy server)
- Serves on port 8080 from /home/phill/ckb-dashboard/
- HTTP polling (no WebSocket — node has WS disabled)
- Proxy architecture (server.js) fixes iOS Safari cross-origin issue
- Published to GitHub: https://github.com/toastmanAu/ckb-node-dashboard
- Phill's GitHub account: toastmanAu
- gh CLI authenticated as toastmanAu

## Fan Controller
- Orange Pi 5 in a 3D printed case with 2-pin 5V fan via MOSFET
- Script: /home/phill/fan-control/fan-control.py
- Using GPIO1_C4 (gpio52, sysfs software PWM at 25Hz)
- DT overlay attempted for hardware PWM1_M2 but u-boot-update didn't apply it correctly
- Service installed and running: sudo systemctl status fan-control
- Temperature curve: off <40°C, 25% at 45°C, linear to 100% at 78°C
- The Orange Pi 5 has a 26-pin header (not 40-pin)
- pwmchip0=PWM2(fd8b0020), pwmchip1=PWM6(febd0020) — neither on header
- Hardware PWM pins on header: GPIO1_C4=PWM1_M2, GPIO1_D3=PWM0_M1

## Orange Pi 5 System Info
- Running Ubuntu 22.04, kernel 5.10.0-1012-rockchip
- Boot: U-Boot + extlinux (/boot/extlinux/extlinux.conf)
- DTB: /lib/firmware/5.10.0-1012-rockchip/device-tree/rockchip/rk3588s-orangepi-5.dtb
- GNOME Shell 42.9, Wayland session
- Vitals GNOME extension installed to ~/.local/share/gnome-shell/extensions/ — needs logout/login to activate

## NerdMiner CKB Adaptation
- Cloning/adapting BitMaker-hub/NerdMiner_v2 for CKB Eaglesong mining
- Repo: https://github.com/toastmanAu/NerdMiner_CKB (already exists)
- Local path: /home/phill/workspace/NerdMiner_CKB/
- Eaglesong: 16×uint32 state, 256-bit rate, 43 rounds, delimiter=0x06
- CKB mining: eaglesong(pow_hash[32] || nonce[16]) → compare against target
- 128-bit nonce (vs Bitcoin's 32-bit) — using uint64_t lo counter + fixed hi
- In progress at session end (writing eaglesong.h)

## About Phill

- Mostly self-taught dev, formal C training 20+ years ago
- Got back into coding 4 years ago via ESP32 + crypto price tracker project
- Deep in Nervos (CKB) community for ~4 years
- Hardware lover — running multiple Pis, 2 Nervos nodes, 1 Bitcoin node
- Motivated by opportunity in the blockchain/crypto space
- Based in Adelaide, Australia (GMT+10:30)

---

## Session Continues — Morning/Midday

### CKB Dashboard Fixes
- Dashboard (port 8080) kept crashing due to EADDRINUSE — crash guard was swallowing the error, creating zombies
- Fixed: EADDRINUSE now triggers clean exit instead of silent continuation
- Heartbeat updated to always `pkill` before restart (avoid port conflicts)
- Dashboard server now at `/home/phill/ckb-dashboard/server.js` (running version)
- Repo at `/home/phill/workspace/ckb-node-dashboard/` — promoted feature-rich version as canonical
- Features: /history endpoint, block time charts, hashrate, batch RPC, /health endpoint
- Pushed to GitHub: toastmanAu/ckb-node-dashboard

### CKB Whale Bot
- Created GitHub repo: https://github.com/toastmanAu/ckb-whale-bot
- Fixed: pool password HTML form ID had spaces ("Poolpassword - Optional") — broke getValue(), password never saved
- Added self-transfer filter: resolves input UTXOs, skips txs where all outputs return to input addresses
- Switched threshold from fixed 10M CKB to **$200,000 USD** using CoinGecko live price (5-min cache)
- Alert now shows USD value (~$X @ $Y/CKB)
- Config at /home/phill/ckb-whale-bot/config.json (gitignored): ckb_rpc, bot_token, chat_id
- Bot token: 8446459270:AAFltgKPOgFc0FX4PjKJNPUxTRoRzayKAlE | Chat: -1001338982855
- Restart: bash /home/phill/ckb-whale-bot/start.sh

### NerdMiner CKB — ESP32 CYD
- Installed PlatformIO Core 6.1.19 in venv at /home/phill/.platformio-venv/
- `pio` symlinked to ~/.local/bin/pio (on PATH)
- Target board: ESP32-2432S028R (CYD — Cheap Yellow Display)
- Build target confirmed working: `pio run -e ESP32-2432S028R`
- Added Worker Name field: pool auth now sends "account.worker" format for ViaBTC
- Default pool changed to ckb.viabtc.com:3333
- Fixed broken password field HTML ID (spaces broke getValue)
- Firmware at .pio/build/ESP32-2432S028R/firmware.bin
- To flash: `pio run -e ESP32-2432S028R -t upload` (need dialout group — Phill must run from terminal)
- To erase+flash: `pio run -e ESP32-2432S028R -t erase && pio run -e ESP32-2432S028R -t upload`
- NerdMiner push blocked by workflow scope — Phill needs: `gh auth refresh -s workflow` then `git push`

### Model Fallback Chain Configured
- Primary: anthropic/claude-sonnet-4-6
- Fallback 1: ckbdev/claude-sonnet-4-6 (shared builders API, key in .env as CKBDEV_API_KEY)
- Fallback 2-4: HuggingFace free (Llama 3.3 70B, DeepSeek V3.2, Qwen3 32B) — no key needed
- CKBDev shared API: https://share-ai.ckbdev.com (Anthropic-compatible, Claude models only, no image gen)
- Model health check script: /home/phill/.openclaw/workspace/scripts/check-models.sh
- Runs hourly via heartbeat — alerts if primary degrades

### OpenAI Key
- Key: sk-proj-Zgm... — billing hard limit reached (no credit), image gen unavailable
- Do not delete — may work again if Phill adds credit

### Image Gen
- Pollinations.ai script: /home/phill/.openclaw/workspace/scripts/gen-image.sh (free, no key)
- Pollinations was down at time of session — try later
- PFP concept for PinchyToastBot: orange lobster popping out of chrome toaster, dark bg, circuit board accents

### dialout Group
- Phill ran `sudo usermod -aG dialout phill` — permanent, takes effect on new login
- Exec sessions may not have it yet until Phill logs out/in
- brltty removed (`sudo apt remove brltty`) — was hijacking CH340 USB serial

### Dashboard Keep-Alive (Recurring Issue)
- Dashboard crashes/dies periodically — heartbeat auto-restarts it
- Suggested making it a systemd service for proper auto-recovery — Phill hasn't done this yet

### Session Wrap / Pre-Compaction State (10:44 AM)

#### Pending / Blocked
- NerdMiner push still blocked → needs `gh auth refresh -s workflow` then `git push` from Phill's terminal
- Dashboard health unconfirmed at compaction time — heartbeat will verify/restart
- PFP for PinchyToastBot not yet generated (Pollinations was unreachable earlier)
- Phill still needs to flash NerdMiner from his terminal (`pio run -e ESP32-2432S028R -t upload`)

#### Binance Trading Bot
- Created at /home/phill/binance-bot/ — Python venv, all deps installed
- Paper trading by default; RSI(14) + EMA50/200 trend filter on 4h candles
- Price must be above EMA-200; RSI < 30 = entry; exits via SL/TP only
- Backtest (Feb 2026): ETH 4h 365d = 66.7% win, +1.31% ROI; BTC 1h 365d = 42.9%, +0.59%
- Bot correctly sitting out current bear market; next: grid strategy for ranging market
- Run: `cd /home/phill/binance-bot && source venv/bin/activate && python bot.py`

#### POS System (ESP32) — Next Step
- Cloudflare Worker needed to track CKB payment confirmations on-chain

#### OpenAI Billing
- Was blocked earlier in session; Phill confirmed resolved ("Sorry limit changed")
- Image gen should be available again via OpenAI key

### NerdMiner CKB Debug (continued)

- Serial output captured: device was failing DNS with "Cannot connect to: stratum+tcp://mining.viabtc.io"
- Root cause: SPIFFS/NVS had full stratum URI saved as pool address — `hostByName()` needs bare hostname only
- Fix: `getCleanHost()` helper strips URI scheme (stratum+tcp://, stratum://, tcp://) and trailing :port
- Also fixed `checkValid()` pointer bug: `memcpy(diff_target, &target, 32)` → `memcpy(diff_target, target, 32)` (&target = address of pointer variable = garbage data)
- Added: settings dump on startup, first-hash debug per job
- Committed: ab7bf05 — needs flash by Phill (`pio run -e ESP32-2432S028R -t upload`)
- After flashing, check serial for subscribe response format — may need further protocol fixes

### NerdMiner CKB — Running! (final debug session)

Fixed 3 more bugs after first flash:
1. CRASH on mining.notify: ViaBTC sends 5 params [job_id, pow_hash, block_height(INT), target, clean_jobs] — code treated integer as char* → null deref crash. Fixed with param count + type detection.
2. mining.set_target unhandled: ViaBTC sends share target as hex string, not numeric difficulty. Added MINING_SET_TARGET to dispatch, parse_mining_set_target() (BE→LE), converts to diff. Pool diff = 79.161211.
3. Leading space in wallet address from SPIFFS. Trimmed at auth time.

Serial confirmed working:
- Connected to mining.viabtc.io:3001 ✓
- Subscribed (extranonce1: 6c969cab, extranonce2_size: 12) ✓
- Authorized as ckb1qyqwueud5e9j3lp3chv8qq820s7lxyggd9usvlg.Wyltek.001 ✓
- Getting jobs every ~6s ✓
- Hashing at ~3 KH/s (both cores) ✓
- No crashes ✓
- Commits: ab7bf05, 678e5e7 — pushed to toastmanAu/NerdMiner_CKB main

Note: `sg dialout -c "pio run..."` works for flashing without sudo

### Fan Controller — Quiet Tuning

Two noise sources fixed:
1. PWM_FREQ_HZ: 25 → 200 Hz (motor coils no longer vibrate at audible frequency)
2. Constant 40%↔42% hunting every 3s → locked steady at 37% with:
   - SPEED_DEADBAND = 4% (ignore target changes < 4%)
   - RAMP_STEP = 6% per poll (smooth transitions, no sudden speed jumps)
   - POLL_INTERVAL: 3s → 5s
   - New curve: (45,0), (50,32), (60,50), (68,72), (75,90), (80,100)

Result: at ~52.7°C, was hunting 39/40/42% every 3s; now locked at 37% indefinitely.
Fan controller has no git repo (/home/phill/fan-control/).

### NerdMiner CKB — Display Investigation (pre-compaction 11:27 AM)

Root causes for display issues identified (no code written yet — next step):

1. **Difficulty shows 0.000000**: `best_diff` starts at 0 until a share is found. Real pool difficulty is in `currentPoolDifficulty` (static double, `mining.cpp` line 169) — not exposed to monitor.cpp. Fix: add `getPoolDifficulty()` getter in mining.cpp/mining.h.

2. **Worker display broken**: `printPoolData()` calls `getPoolData()` which hits Public-Pool.io REST API → fails for ViaBTC → returns zeroed defaults (workersCount=0, workersHash="", bestDifficulty=""). Fix: when API fails, read from local `Settings.WorkerName` + `Settings.PoolAddress` instead.

3. **"Public-Pool.io" label baked in**: `bottonPoolScreen` bitmap in `images_bottom_320_70.h` has the text burned in. Fix: draw text overlay using TFT primitives to cover/replace it.

4. **CKB logo**: Background images are bitmaps in header files. Fix: draw programmatically using TFT hexagon + "CKB" text overlay.

Files read this session: `esp23_2432s028r.cpp`, `nvMemory.cpp`, `storage.h`, `eaglesong.cpp/h`, `mining.h`, `monitor.cpp/h`

Implementation plan:
- Add `getPoolDifficulty()` in mining.cpp + mining.h
- Update `printPoolData()` in monitor.cpp to use local settings as fallback
- Add `drawCKBLogo()` using TFT primitives
- Draw pool name text overlay on bottom bar
- Build: `cd /home/phill/workspace/NerdMiner_CKB && sg dialout -c "/home/phill/.platformio-venv/bin/pio run -e ESP32-2432S028R -t upload 2>&1"`

### State at Pre-Compaction (11:27 AM)

**Pending next steps:**
- Implement NerdMiner display fixes (difficulty, worker info, CKB logo)
- Generate PFP for PinchyToastBot (orange lobster + chrome toaster concept, pollinations.ai)
- Verify dashboard health: `curl -s http://localhost:8080/health`
- Check whale bot: `ps aux | grep whale-bot | grep -v grep`
- POS ESP32: Cloudflare Worker for CKB payment confirmation tracking (not started)

**Key file paths:**
- NerdMiner: `/home/phill/workspace/NerdMiner_CKB/`
- Fan controller: `/home/phill/fan-control/fan-control.py` (no git repo)
- Dashboard: `/home/phill/ckb-dashboard/server.js`
- Whale bot: `/home/phill/ckb-whale-bot/whale-bot.js`

---

## Afternoon Session (3:34 PM)

### NerdMiner CKB Display Fixes
- Rotation fixed: `setRotation(1)` → `setRotation(3)` (USB at top mount orientation)
- Nervos N-mark logo: replaced hexagon with official N-mark traced from Phill's SVG (RGBA PNG, alpha=255 = strokes)
  - Two fillRect + two fillTriangle calls, CKB green #3CC68A, sprite coords (68,15) 54×54 stroke=14px
- Pushed commit `4886c01` to toastmanAu/NerdMiner_CKB main

### PFP Generated
- Extracted Nervos N-mark from SVG Phill sent (application/xml with embedded base64 PNG)
- Generated `ckb-pfp.png` — N-mark in CKB green on dark bg with radial glow, 512×512
- Sent to Phill via Telegram

### Neuron arm64 AppImage
- Built successfully on this Pi 5: `Neuron-v0.204.0-arm64.AppImage` (192MB, ELF aarch64)
- PR #3441 submitted to nervosnetwork/neuron with full documentation
- Key flag: `npx electron-builder --linux --arm64 --config.npmRebuild=false`

### CKB-Node Repo Improvements (toastmanAu/CKB-Node)
- Rewrote `update_ckb.sh`: auto-detects latest version, arch detection, service mgmt, backup, colour output
- Added `setup.sh`: one-shot fresh image setup — installs CKB, creates systemd service, disables WiFi PS, configures sudoers
- Cleaned up README with proper markdown, parts table, setup instructions

### UV Index Tracker
- Built `/home/phill/uv-tracker/index.html` — BOM API, no key required
- Features: location search by name/postcode, 7-day UV forecast, gauge bar, advice, astronomy panel
- Serving on port 9988 via `setsid python3 -m http.server 9988`
- Added to HEARTBEAT.md for auto-restart

### CKB Node (Orange Pi 3B) — ckbnode
- Got SSH access: `ssh ckbnode` (orangepi@192.168.68.87)
- Identified: RK3566, kernel 5.10.160, CKB v0.202.0 running as process
- WiFi: power save disabled (modprobe.d + udev + NM)
- NOPASSWD sudo configured via /etc/sudoers.d/orangepi
- CKB updated 0.202.0 → 0.204.0 via setup script
- CKB now runs as systemd service (auto-start, restart on failure, 60s graceful shutdown)
- DSI display intel gathered: `raspberrypi,7inch-touchscreen-panel` on DSI1 (fe070000), i2c1 (fe5a0000), ft5426 touch @ 0x38
- Overlay: `rk356x-raspi-7inch-touchscreen` — just enables pre-defined DTB nodes
- Mainline driver exists since 5.15 — portabe to Armbian 6.15

### OpenClaw Browser Config
- Set `browser.defaultProfile` = `openclaw` (managed isolated Brave profile)
- Set `browser.executablePath` = `/usr/bin/brave-browser`
- Restarted gateway to apply

### openscad-stl Skill Created
- Installed OpenSCAD 2021.01 system-wide
- Created skill at `~/.npm-global/lib/node_modules/openclaw/skills/openscad-stl/`
- SKILL.md + references/openscad-patterns.md (enclosure, screw boss, snap fit, DIN rail, hex grid, threads)
- STL output dir: `/home/phill/.openclaw/workspace/stl/`
- Triggers on any 3D print / STL design request

### Android STL Viewer App — Planned
- Plan: React Native + three.js STL viewer, receives files via share sheet or Pi local server
- Build via Expo EAS (free tier cloud build → APK)
- Waiting on Phill to create Expo account and share username
- Will build code in meantime

### Second Orange Pi 3B — Incoming
- Phill setting up another OPi3B as agent host
- Same SSH key will be used
- Phill will send IP when ready
- Plan: install OpenClaw, fix GPU/DSI/WiFi issues, write Armbian 6.15 DSI overlay

### ckbnode SSH Config
- Added to `~/.ssh/config` as `Host ckbnode` → orangepi@192.168.68.87

---

## Late Afternoon Session (4:50 PM)

### Armbian OPi3B (opi3b-armbian) — Second Board Setup
- IP: 192.168.68.93 · User: obi3boc · SSH alias: opi3b-armbian · NOPASSWD sudo
- Kernel: 6.12.58-current-rockchip64 (switched from 6.19-rc6-edge)
- Weston running via LightDM autologin → weston session
- HDMI: working, extended desktop planned
- DSI: card1-DSI-1 connected, panel_raspberrypi_touchscreen loaded, both i2c devs detected (1-0038 touch, 1-0045 bridge)
- DSI still showing white/dark — ATTINY88 probe defer (-517) resolves but weston may not init panel
- Display: KeDei 5" DSI V1.1 with Lattice FPGA bridge (emulates TC358762 + ATTINY88 protocol)
- Overlay: /boot/overlay-user/opi3b-waveshare5-dsi.dts (.dtbo) — uses raspberrypi,7inch-touchscreen-panel on i2c1@0x45
- armbianEnv: user_overlays=opi3b-waveshare5-dsi, fdtfile=rk3566-orangepi-3b-v1.1.dtb
- Weston config: /home/obi3boc/.config/weston.ini — dual output HDMI-A-1 + DSI-1
- LightDM: autologin to weston session, /etc/lightdm/lightdm.conf.d/10-autologin.conf
- Node.js: 20.x installed via NodeSource
- OpenClaw install: attempted via npm pack+scp (22MB tgz), install still in progress/pending

### NerdMiner CKB Display Fixes Round 2
- Black fill behind N-mark: fillRect(40,8,85,105,BLACK) before drawing N-mark
- N-mark repositioned to sprite (47,12), 54×54, stroke=14
- Pool bar: color565(0,191,191) cyan + black text (was wrong colour 0x0E3E)
- Committed f8f51a1, pushed to toastmanAu/NerdMiner_CKB main

### openscad-stl Skill
- Installed OpenSCAD 2021.01 system-wide
- Skill at ~/.npm-global/lib/node_modules/openclaw/skills/openscad-stl/
- Output dir: /home/phill/.openclaw/workspace/stl/

### OPi3B Case STL
- Designed for 50mm fan, all port cutouts, snap-fit lid
- Sent to Telegram (message 675)
- SCAD source: /home/phill/.openclaw/workspace/stl/opi3b-case.scad
- NOTE: mounting hole spacing needs verification before printing (used 52×37mm)

### Android STL Viewer App
- Plan: React Native + three.js WebView STL viewer + Pi local server push
- Build via Expo EAS free tier
- Waiting on Phill to create Expo account and share username

### DSI Investigation Notes (important for future)
- KeDei display uses Lattice FPGA programmed to emulate TC358762 DSI-DPI bridge
- ATTINY88 at i2c 0x45 handles i2c control, Lattice FPGA is DSI peripheral
- mainline panel-raspberrypi-touchscreen driver IS compatible (confirmed by ckbnode)
- BSP overlay (rk356x-raspi-7inch-touchscreen) just enables pre-defined DTB nodes
- The -517 probe defer at boot is normal and self-resolves
- Key: panel node must be on i2c1 (fe5a0000) at 0x45, DSI1 (fe070000), DPHY1 (fe860000), VP1
- weston.ini: [output] name=DSI-1 mode=800x480 (no transform needed)
- Weston log at /tmp/weston.log
- dsi-panel-reprobe.service installed but not needed — weston handles via DRM

### opi3b-armbian SSH key
- Added to /home/obi3boc/.ssh/authorized_keys during SD boot session
- Persisted to eMMC — key: kernel@oPI5-oc ed25519

---

## Session ~5:01 PM — Solo Mining Setup

### CKB Solo Mining via Stratum Proxy
- ckb.toml block_assembler configured: args = "0xee678da64b28fc31c5d87000ea7c3df31108" (correct 20-byte lock args)
- ckb.service updated: ExecStart now includes `--ba-advanced` flag (required for non-standard lock format)
- Miner RPC confirmed working: get_block_template returns 560 CKB reward at current height
- Solo proxy running: node solo-proxy.js, stratum on 192.168.68.82:3333, stats on localhost:8081
- config.json updated: mode="solo", node.host=192.168.68.87, node.port=8114, coinbase=Phill's address
- HEARTBEAT.md updated to reflect solo mode monitoring

### NerdMiner CKB — Default Pool Updated
- storage.h defaults changed: DEFAULT_POOLURL = "192.168.68.82", DEFAULT_WALLETID = Phill's CKB address
- Flashed to ESP32-2432S028R successfully (66s, SUCCESS)
- Committed and pushed: 8bfd1b8 "Set default pool to solo proxy (192.168.68.82:3333)"
- Note: existing devices with SPIFFS config need manual pool update in web UI to switch

### Lock Args Derivation Note
- CKB address: ckb1qyqwueud5e9j3lp3chv8qq820s7lxyggd9usvlg
- Short address format: payload[0]=0x01 (format byte), payload[1:21] = actual 20-byte lock args
- Correct lock args: 0xee678da64b28fc31c5d87000ea7c3df31108 (NOT 0x00ee678... — the 0x00 was the format byte)

### Stray TOML Fix
- ckb.toml had stray line `["http://127.0.0.1:8888"]` at line 195 — caused DATAERR crash loop
- Fixed by deleting that line; node recovered and is active

---

## Session ~7:00–10:19 PM — Major Expansion

### Stratum Proxy v1.0.0 Released
- Proxy ran stable for 1h10m — met release threshold
- Pushed v1.0.0 tag to toastmanAu/ckb-stratum-proxy
- README rewritten, config.example.json added
- Hashrate fix: switched to rolling 5-minute window (was all-time average — massively undercounting)
- Stale share double-count bug fixed
- CKBox entered connect-disconnect loop after proxy restart — needs physical power cycle to recover

### Neuron arm64 AppImage Testing
- PR #3441 on nervosnetwork/neuron — adds Linux arm64 AppImage support
- Built: Neuron-v0.204.0-arm64.AppImage (192MB) at /home/phill/neuron-build/release/
- Tested on Pi 5 via Xvfb — welcome screen renders correctly ✅
- Tested on ckbnode (OPi3B) — app launches, connects to local CKB node ✅
- Issues found: libz.so symlink needed on minimal systems, GPU falls back to SwiftShader on BSP 5.10
- Comment posted to PR: https://github.com/nervosnetwork/neuron/pull/3441#issuecomment-3940599556

### OPi3B (new board) Setup
- Flashed with official Orange Pi BSP 5.10.160 image
- SSH: orangepi@192.168.68.93, alias opi3b-armbian
- SSH key delivery: served pubkey via HTTP from Pi5 (curl http://192.168.68.82:9999/authorized_keys)
- NOPASSWD sudo configured
- Node.js 22 installed
- OpenClaw 2026.2.21-2 installed (from tarball — npm registry has stub 0.0.1)
- Gateway running: LAN mode, port 18789, token: 3b8ab4efee68e4f69d0a129aa4893174e8e728e6db30d7ac
- Mali G52 GPU confirmed: /dev/mali0, libmali.so present, rockchip_dri.so in DRI path
- libdrm-cursor preloaded in X (LD_PRELOAD=libdrm-cursor.so.1)
- WiFi power save disabled + udev rule for persistence
- Hostname set to opi3b-oc
- Running from eMMC (mmcblk0 = MMC type, 233GB) — SD card (mmcblk1 = 58GB) just present
- linger enabled for systemd user session
- OpenClaw console setup still needed at physical terminal (LAN gateway accessible at 192.168.68.93:18789)
- User in config is orangepi (NOT obi3boc — that was armbian default on old image)

### N100 Mini PC
- Hostname: wyltek-n100, IP: 192.168.68.91, user: phill
- SSH alias: n100
- Intel N100, 4 cores, 15GB RAM, 237GB free, Ubuntu 24.04, x86_64
- Has existing CKB node at ~/ckb/
- NOPASSWD sudo configured (Warriors22$)
- Node.js 22 installed, Rust 1.93.1 installed
- Trading dashboard deployed: http://192.168.68.91:9090 (systemd service)
  - Source: /home/phill/workspace/trading-dashboard/ (Pi5) + ~/trading-dashboard/ (N100)
  - SQLite DB at ~/binance-bot/data/trades.db
  - Features: stats cards, cumulative P&L chart, win/loss donut, open/closed positions, trade history

### Trading Bot (Binance)
- Copied to N100: ~/binance-bot/
- Python 3.12 venv recreated (Pi5 used 3.14 — incompatible)
- Deps installed: ccxt, pandas, numpy, python-dotenv, aiohttp, requests
- Config: config.py — paper mode default, needs BINANCE_API_KEY + BINANCE_API_SECRET for live
- Telegram alerts: TELEGRAM_TOKEN + TELEGRAM_CHAT_ID env vars
- Status: ready to run, awaiting API keys decision (paper vs live)

### Fiber Network Node
- Latest release: v0.7.0 (x86_64 only — no aarch64 binary)
- Building from source on BOTH Pi5 (/home/phill/fiber/) AND N100 (~/fiber/)
- Rust installed on Pi5 for this purpose
- Pi5 build: slower (arm64 cross-compile not needed, native aarch64)
- N100 build: x86_64, faster
- ckbnode setup ready:
  - Directory: /home/orangepi/fiber/{data,config,logs}
  - Config: /home/orangepi/fiber/config/config.yml (mainnet, RPC 127.0.0.1:8227, CKB at 127.0.0.1:8114)
  - systemd service: /etc/systemd/system/fiber.service (installed, not started — awaiting binary)
  - Port 8228 (P2P) free and ready
- Fiber wallet address generated for ckbnode: ckb1qzda0cr08m85hc8jlnfp3zer7xulejywt49kt2rr0vthywaa50xwsqtj5ses5f88ggyeggrz7f9zh0kch40ctxsas20ze
- Private key temporarily on Pi5 — move to ckbnode securely once binary ready
- Phill sending funds to this address for channel liquidity

### orangepi-build Downloaded
- Cloned to /home/phill/orangepi-build/ (depth=1)
- Key findings: builder needs x86 host to run (Docker + chroot cross-compile)
- N100 is the right machine to run image builds
- OPi Zero 3: H618 SoC, kernel targets: current (5.4) or next (available branches)
- OPi 3B: RK3566, legacy branch = 5.10 BSP, current = 6.6
- Plan: build custom images for Zero 3 fleet + future boards with CKB node + Neuron pre-installed

### Toastman PFP
- Requested but rate-limited on Gemini free tier
- Background retry script running at /tmp/gen-toastman.py (PID ~170691)
- Will auto-send when generated

### PFP Generation Note
- Gemini image gen: gemini-2.0-flash-exp-image-generation works (free tier)
- imagen-4.0 requires billing — not available
- Rate limit: hits 429 after ~2 requests per minute — need 30-60s backoff between attempts

### Key Passwords / Access
- Warriors22$ = OPi3B (orangepi) + N100 (phill) sudo password
