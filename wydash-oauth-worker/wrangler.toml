name = "wydash-oauth"
main = "src/index.js"
compatibility_date = "2024-01-01"

[vars]
# Comma-separated list of origins allowed to use this worker
# Add your WyDash IPs/domains here
ALLOWED_ORIGINS = "http://192.168.68.98:9999,http://localhost:9999"

# Set secrets via: wrangler secret put GH_CLIENT_ID
# and:             wrangler secret put GH_CLIENT_SECRET
